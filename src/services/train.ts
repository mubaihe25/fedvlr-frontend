import { TrainConfig } from '../types/train';

export const startTrain = async (config: TrainConfig) => {
  // TODO: Implement actual API call
  console.log('Starting train with config:', config);
  return { taskId: 'mock-task-id' };
};
