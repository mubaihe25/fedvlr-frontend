# FedVLR-API base URL used by the frontend.
# Leave this unset in local Vite dev mode so requests go through /api.
# VITE_API_BASE_URL="/api"

# Optional: override the dev proxy target separately.
# VITE_API_PROXY_TARGET="http://localhost:8000"
